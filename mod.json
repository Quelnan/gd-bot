{
  "geode": "4.10.0",
  "gd": { "win": "2.2074" },
  "id": "developer.gdbot",
  "name": "GD AutoBot Trainer",
  "version": "v1.0.0",
  "developer": "Developer",
  "description": "Speedhack training bot that learns by mutating inputs and replays the best path at normal speed",
  "settings": {
    "enabled": {
      "name": "Enabled",
      "description": "Enable the bot",
      "type": "bool",
      "default": false
    },
    "mode": {
      "name": "Mode",
      "description": "0=Off, 1=Train+Verify",
      "type": "int",
      "default": 1,
      "min": 0,
      "max": 1
    },
    "train-timescale": {
      "name": "Train Speed (timescale)",
      "description": "Higher = faster attempts, but may reduce determinism",
      "type": "float",
      "default": 3.0,
      "min": 1.0,
      "max": 10.0
    },
    "restart-delay-frames": {
      "name": "Restart Delay (frames)",
      "description": "Frames to wait after death before resetting",
      "type": "int",
      "default": 10,
      "min": 0,
      "max": 120
    },
    "max-frames": {
      "name": "Max Frames Per Attempt",
      "description": "Stops runaway attempts; larger for long levels",
      "type": "int",
      "default": 300000,
      "min": 10000,
      "max": 1000000
    },
    "mutation-window": {
      "name": "Mutation Window (frames)",
      "description": "Mutations concentrate around death frame ± window",
      "type": "int",
      "default": 240,
      "min": 30,
      "max": 2000
    },
    "mutation-rate": {
      "name": "Mutation Rate",
      "description": "Probability of flipping a frame input inside the mutation window",
      "type": "float",
      "default": 0.02,
      "min": 0.0,
      "max": 0.5
    },
    "seed-tap-rate": {
      "name": "Seed Tap Density",
      "description": "How often random taps are inserted in a new genome (lower = fewer taps)",
      "type": "float",
      "default": 0.003,
      "min": 0.0005,
      "max": 0.05
    },
    "tap-length-frames": {
      "name": "Tap Length (frames)",
      "description": "How long each inserted tap holds",
      "type": "int",
      "default": 2,
      "min": 1,
      "max": 20
    },
    "log-status": {
      "name": "Log Status",
      "description": "Print periodic training status to console",
      "type": "bool",
      "default": true
    }
  }
}
